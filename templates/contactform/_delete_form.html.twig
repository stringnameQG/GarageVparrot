<form method="post" action="{{ path('app_contactform_delete', {'id': contactform.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contactform.id) }}">
    <button class="formulaire-affichage__btn__supprime">Supprimer</button>
</form>
